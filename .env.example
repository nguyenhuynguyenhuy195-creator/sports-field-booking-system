FLASK_APP=run.py
# Use development for local work or production for the secure deployment profile.
# DEBUG is selected by APP_ENV; do not enable FLASK_DEBUG in production.
APP_ENV=development
# Required. Generate a private value for each environment and never commit it.
# Example: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=

DB_DRIVER=ODBC Driver 17 for SQL Server
DB_SERVER=localhost
DB_NAME=sports_field_booking
DB_USERNAME=
DB_PASSWORD=
DB_TRUSTED_CONNECTION=yes
DB_TRUST_SERVER_CERTIFICATE=yes

# Phase 1.3B1: public Nominatim is suitable only for low-volume student/demo use.
# Use an identifying User-Agent, keep geocoding server-side, and change the
# provider/base URL before production-scale traffic. No API secret is required.
GEOCODING_PROVIDER=nominatim
NOMINATIM_BASE_URL=https://nominatim.openstreetmap.org
NOMINATIM_USER_AGENT=sports-field-booking-student-demo/1.0 (https://github.com/nguyenhuynguyenhuy195-creator/sports-field-booking-system)
GEOCODING_TIMEOUT_SECONDS=5
GEOCODING_CACHE_TTL_SECONDS=86400

# OSM-compatible browser tiles are independent from the geocoding service.
MAP_TILE_URL=https://tile.openstreetmap.org/{z}/{x}/{y}.png

# Payment providers:
# MOCK        = always-on fallback for dev/test; no external network calls.
# VNPAY       = real Sandbox provider once VNPAY_ENABLED=true and the
#               credentials below are filled in from your own VNPAY
#               merchant sandbox account. Never commit real values here.
# MoMo        = legacy, permanently disabled at the code level
#               (MOMO_ENABLED is hardcoded false in config.py regardless of
#               this file); the keys below only document the old
#               integration and are not honored by the running app.
MOMO_ENABLED=false
MOMO_PARTNER_CODE=
MOMO_ACCESS_KEY=
MOMO_SECRET_KEY=
MOMO_BASE_URL=https://test-payment.momo.vn
MOMO_REDIRECT_URL=https://your-public-domain.example/payments/momo/return
MOMO_IPN_URL=https://your-public-domain.example/payments/momo/ipn
MOMO_TIMEOUT_SECONDS=30

VNPAY_ENABLED=false
VNPAY_TMN_CODE=
VNPAY_HASH_SECRET=
VNPAY_PAYMENT_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNPAY_API_URL=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
VNPAY_RETURN_URL=https://your-public-domain.example/payments/vnpay/return
VNPAY_IPN_URL=https://your-public-domain.example/payments/vnpay/ipn
VNPAY_TIMEOUT_SECONDS=30
VNPAY_VERSION=2.1.0
VNPAY_LOCALE=vn
# Non-secret defaults used only when submitting a VNPAY refund request.
VNPAY_REFUND_CREATE_BY=system
VNPAY_REFUND_IP_ADDR=127.0.0.1

# Chatbot (trợ lý ảo RAG). Tắt mặc định.
# Ứng dụng vẫn khởi động bình thường khi CHATBOT_ENABLED=false hoặc khi thiếu
# GEMINI_API_KEY; lúc đó chatbot ở trạng thái không khả dụng, không phải lỗi.
# Lấy khóa tại https://aistudio.google.com/apikey. Không commit khóa thật.
CHATBOT_ENABLED=false
GEMINI_API_KEY=
# gemini-2.5-flash-lite đã ngừng cấp cho khóa mới (API trả 404).
CHATBOT_MODEL=gemini-3.5-flash-lite
# Gemini 3.x dùng thinking_level thay cho thinking_budget. Để trống = bỏ hẳn.
CHATBOT_THINKING_LEVEL=low
CHATBOT_EMBEDDING_MODEL=gemini-embedding-001
CHATBOT_EMBEDDING_DIMENSIONS=768
# Tham số cắt đoạn và truy xuất kho kiến thức trong docs/chatbot/.
CHATBOT_CHUNK_SIZE=1000
CHATBOT_CHUNK_OVERLAP=120
CHATBOT_RETRIEVAL_TOP_K=4
# Ngưỡng chặn bằng chứng yếu. Đã đo thật ngày 13/09/2026 với
# gemini-embedding-001 @768d, 27 câu hỏi tiếng Việt:
#   liên quan   0.769-0.870 | mơ hồ 0.676-0.746 | không liên quan 0.565-0.650
CHATBOT_MIN_RELEVANCE_SCORE=0.70
CHATBOT_STRONG_RELEVANCE_SCORE=0.78
CHATBOT_MAX_SOURCES=3
CHATBOT_TIMEOUT_SECONDS=20
